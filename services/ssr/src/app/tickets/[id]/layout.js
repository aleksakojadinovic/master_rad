import PageContainer from '@/components/PageContainer/PageContainer';
import React from 'react';

export default function TicketViewLayout({ children }) {
  return <PageContainer>{children}</PageContainer>;
}
